# Runbook: Linux Service Monitor

実際に発生した問題と対応記録。

## 1. Tomcatダウンロード時の404エラー

**症状**: `curl -O` で取得したtar.gzがgzip形式として認識されず解凍失敗。

**原因**: 指定したTomcatバージョン(10.1.34)がApacheミラーサイトから既に削除されていた。ダウンロードされたファイルの中身は404 Not FoundのHTMLページだった。

**対応**: `curl -s https://dlcdn.apache.org/tomcat/tomcat-10/ | grep -oP 'v10\.1\.\d+' | sort -V | tail -5` で現在有効なバージョン一覧を取得し、実在するバージョン(10.1.59)を確認してから再取得。

**学び**: バージョン番号を固定したダウンロードスクリプトは、配布元のバージョン整理により突然失敗しうる。動的にバージョンを確認する仕組みが望ましい。

## 2. ユーザー作成時のタイプミスによる連鎖障害

**症状**: `chown`が「No such file or directory」で失敗。

**原因**: `useradd`実行時に`/opt/tomcat`と入力すべきところを`/opt/tomact`とタイプミス。存在しないはずのディレクトリエラーの真因はスペルミスだった。

**対応**: 誤って作成したユーザーとディレクトリを削除し、入力を確認しながら再実行。

**学び**: コマンド実行後は必ず出力結果を目視確認する。特にディレクトリ名やパスは打ち間違いに気づきにくい。

## 3. sudo経由でのワイルドカード展開失敗

**症状**: `sudo chmod +x /opt/tomcat/bin/*.sh` が「No such file or directory」で失敗。

**原因**: ワイルドカード`*.sh`は`sudo`昇格前の実行ユーザー権限で展開される。対象ディレクトリの権限が700(tomcatユーザーのみ読み取り可)だったため、ec2-userのシェルがワイルドカードを展開できず、文字列そのままがコマンドに渡された。

**対応**: `sudo bash -c 'chmod +x /opt/tomcat/bin/*.sh'` のように、root権限のサブシェル内でワイルドカードを展開させる方式に変更。

**学び**: sudoとワイルドカードの組み合わせは、対象ディレクトリへのアクセス権限次第で静かに失敗することがある。

## 4. ディレクトリ権限に起因するアクセス拒否(/opt/tomcat/bin)

**症状**: `/opt/tomcat`直下は閲覧できるが、`/opt/tomcat/bin`配下だけ「Permission denied」で入れない。

**原因**: `chmod 755 /opt/tomcat`は対象ディレクトリ自身にしか効かず、再帰的に子ディレクトリまでは適用されない。tarから展開された`bin`ディレクトリは元々750(その他ユーザーはx権限なし)のままだった。

**対応**: `sudo chmod -R 755 /opt/tomcat` で再帰的に権限を修正。

**学び**: `chmod`は対象を明示しない限り再帰しない。ディレクトリ階層全体に権限変更が必要な場合は`-R`を忘れないこと。

## 5. ヘルスチェックロジックの誤検知(403を異常と誤判定)

**症状**: Apache・Tomcatともに`active`状態にもかかわらず、監視スクリプトが「down」と誤検知し、SNS通知が発報された。

**原因**: `/var/www/html`にデフォルトページが存在せず、Apacheが403 Forbiddenを返していた。スクリプトはHTTPステータス200のみを「正常」と判定するロジックだったため、プロセス自体は正常に応答していたにもかかわらず異常と誤判定した。

**対応**:
1. `/var/www/html/index.html`を配置し暫定対応
2. 判定ロジックを「curlが完全に接続できない場合(ステータス000)のみ異常」に変更し、403/404等の「サーバーが応答している」状態は正常として扱うよう修正

**学び**: ヘルスチェックの判定基準の設計そのものが監視システムの信頼性を左右する。誤検知が続くと本当のアラートが軽視される「オオカミ少年」状態を招く。

## 6. systemd「active」表示と実際の稼働状態の乖離

**症状**: `server.xml`を意図的に破損させてTomcatを再起動したところ、`systemctl is-active`は`active`を返したが、実際にはプロセスが存在せず、8080ポートへの接続もできなかった。

**原因**: `Type=forking`のsystemdユニットは、起動スクリプト(`startup.sh`)がプロセスをforkして正常終了した時点で「起動成功」と判定する。その後Tomcat内部でXML解析エラーが発生してJavaプロセスがクラッシュしても、systemdはそれを検知しない。

**対応**: プロセス死活確認(`systemctl is-active`)だけでなく、アプリケーション層のHTTP応答確認(`curl`)を併用することで、この種の「見せかけのactive」を検出できることを確認した。

**学び**: systemdのステータス表示はあくまで起動スクリプトの終了コードに基づくものであり、アプリケーションが実際に正常稼働していることを保証しない。多層的なヘルスチェックが必要。

## 7. IAM権限不足(CreateTopic)

**症状**: `aws sns create-topic`実行時に`AuthorizationError`。

**原因**: EC2に付与したIAM Roleには`sns:Publish`権限のみを付与しており、`sns:CreateTopic`権限が含まれていなかった。

**対応**: Policyに`sns:CreateTopic`、`sns:Subscribe`、`sns:GetTopicAttributes`を追加。

**学び**: 本来はSNSトピックの作成はIaC(Terraform/CloudFormation)側で事前定義し、EC2実行環境には`sns:Publish`のみを付与するのが最小権限の観点から望ましい。今回は検証を優先し権限を追加したが、今後の改善点として認識している。

## 8. cronie未インストール、およびcrontab編集時のエディタ誤操作

**症状**: `systemctl enable --now crond`が「Unit file crond.service does not exist」で失敗。crontab編集時に誤ってvi(デフォルトエディタ)を操作しファイル内容を破損。

**原因**: Amazon Linux 2023にはcron関連パッケージ(cronie)がデフォルトでインストールされていない。また `sudo crontab -e` は環境変数 `EDITOR` を引き継がずviが起動していた。

**対応**: `sudo dnf install cronie -y` でインストール後、`export EDITOR=nano` と `sudo -E crontab -e` (`-E`で環境変数を引き継ぐ)でnanoを使用するよう変更。

**学び**: ディストリビューションによって標準搭載パッケージが異なる。「入っているはず」という前提を置かず、都度確認する習慣が必要。

## 9. Apacheログディレクトリへのアクセス権限(analyze_log.sh)

**症状**: `analyze_log.sh`実行時、`awk`/`grep`が`/var/log/httpd/access_log`に対して「Permission denied」。

**原因**: ログファイル自体は644(誰でも読み取り可)だったが、親ディレクトリ`/var/log/httpd`が700(rootのみアクセス可)だったため、ファイル権限に関わらず到達できなかった。

**対応**: ログを読み取る処理にのみ`sudo`を付与(スクリプト全体をsudoで実行するのではなく、必要な箇所だけ権限昇格する設計とした)。

**学び**: ファイルへのアクセス可否は、そのファイル自身の権限だけでなく、パス上の全ディレクトリの実行権限(x)にも依存する。
